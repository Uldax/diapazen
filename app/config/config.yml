imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: services.yml }
    - { resource: "@PollBundle/Resources/config/services.yml" }
    - { resource: "@UserBundle/Resources/config/services.yml" }

# Put parameters here that don't need to change on each machine where the app is deployed
# http://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration
parameters:
    locale: en

framework:
    #esi:             ~
    #translator:      { fallbacks: ["%locale%"] }
    secret:          "%secret%"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    validation:      { enable_annotations: true }
    #serializer:      { enable_annotations: true }
    templating:
        engines: ['twig']
        #assets_version: SomeVersionScheme
    default_locale:  "%locale%"
    trusted_hosts:   ~
    trusted_proxies: ~
    session:
        # handler_id set to null will use default session handler from php.ini
        handler_id:  ~
    fragments:       ~
    http_method_override: true

# Twig Configuration
twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"
    globals:
        fos_csrf_provider: "@form.csrf_provider"

# Doctrine Configuration
doctrine:
    dbal:
        driver:   pdo_mysql
        host:     "%database_host%"
        port:     "%database_port%"
        dbname:   "%database_name%"
        user:     "%database_user%"
        password: "%database_password%"
        charset:  UTF8
        # if using pdo_sqlite as your database driver:
        #   1. add the path in parameters.yml
        #     e.g. database_path: "%kernel.root_dir%/data/data.db3"
        #   2. Uncomment database_path in parameters.yml.dist
        #   3. Uncomment next line:
        #     path:     "%database_path%"

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        naming_strategy: doctrine.orm.naming_strategy.underscore
        auto_mapping: true

# Swiftmailer Configuration
swiftmailer:
    transport:  smtp
    host:       in-v3.mailjet.com
#    port:      587
    encryption: ssl
    auth_mode:  login
    username:   119ed70332fb8739bcd460e01b052a1a
    password:   fd286a54665a84519db2b6f37fa317f7
#    spool:     { type: memory }

#FOS User bundle configuration
fos_user:
    db_driver: orm
    firewall_name: secure_area
    user_class: UserBundle\Entity\DiapazenUser
    registration:
        confirmation:
            enabled:    false


hwi_oauth:
    # name of the firewall in which this bundle is active, this setting MUST be set
    firewall_name: secure_area

    http_client:
        verify_peer: false
        # Setting allowing you to turn off SSL verification, by default: true

    #The connect section connects HWIOAuthBundle to the registration system of Symfony.
    connect:
        confirmation: true
        #account_connector: hwi_oauth.user.provider.fosub_bridge
        #registration_form_handler: hwi_oauth.registration.form.handler.fosub_bridge
        #registration_form: fos_user.registration.form

    resource_owners:

        facebook:
            type: facebook
            client_id: %facebook_client_id%
            client_secret: %facebook_client_secret%
            scope: Email
            infos_url:     "https://graph.facebook.com/me?fields=username,name,Email,picture.type(large)"
            paths:
                email:          Email
                profilepicture: picture.data.url
            options:
                #dialog is optimized for popup window
                display: popup

        google:
            type: google
            client_id: %google_client_id%
            client_secret: %google_client_secret%
            scope: "https://www.googleapis.com/auth/userinfo.Email https://www.googleapis.com/auth/userinfo.profile"
            paths:
                email:           Email
                profilepicture:  picture

        linkedin:
            type: linkedin
            client_id: %linkedin_client_id%
            client_secret: %linkedin_client_secret%
            scope: r_fullprofile
            paths:
                email:           Email
                profilepicture:  picture

     #Since FOSUserBundle were used as user provider, fosub section were added.
    fosub:
        # try 30 times to check if a username is available (foo, foo1, foo2 etc)
        username_iterations: 30

        # mapping between resource owners (see below) and properties
        properties:
            google: githubID
            facebook: githubID
            linkedin: githubID
